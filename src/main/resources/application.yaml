server:
  port: 8082
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jpiecuch/gym-home/
          search-paths: config/configs, config/configs/{application}
security:
  user:
    password: 'mysecret'